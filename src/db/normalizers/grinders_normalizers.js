/**
 * Normalizers for any queries regarding grinders.
 */
export const normalizeGrinders = (data) => {
  return data;
};

export const normalizeGrinderById = (data) => {
  return data;
};

export const normalizeGrindersByName = (data) => {
  return data;
};

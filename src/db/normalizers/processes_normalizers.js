/**
 * Normalizers for any queries regarding processes.
 */
export const normalizeProcesses = (data) => {
  return data;
};

export const normalizeProcessById = (data) => {
  return data;
};

export const normalizeProcessesByName = (data) => {
  return data;
};

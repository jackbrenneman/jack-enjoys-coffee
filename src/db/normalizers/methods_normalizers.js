/**
 * Normalizers for any queries regarding methods.
 */
export const normalizeMethods = (data) => {
  return data;
};

export const normalizeMethodById = (data) => {
  return data;
};

export const normalizeMethodsByName = (data) => {
  return data;
};
